pipeline {
  agent any
  environment {
<<<<<<< HEAD
    APP_HOST = "54.198.173.58"                 // <-- change to your App EC2 public IP
    SSH_KEY  = "/var/lib/jenkins/.ssh/app.pem" // key Jenkins uses to SSH to App EC2
=======
    APP_HOST = "54.198.173.58"  // put your app_public_ip here
>>>>>>> 0eaed00 (jenkinsfile)
  }
  stages {
    stage('Checkout') {
      steps {
        git url: '__YOUR_REPO_URL__', branch: 'main'
      }
    }
    stage('Build with Maven') {
      steps {
        sh 'cd hello-web && mvn -B -DskipTests clean package'
      }
    }
    stage('Deploy with Ansible') {
      steps {
        sh '''
        cat > inventory.ini <<EOF
        [app]
        app1 ansible_host=${APP_HOST} ansible_user=ubuntu ansible_ssh_private_key_file=/var/lib/jenkins/.ssh/app.pem
        EOF

        cat > deploy.yml <<EOF
        - hosts: app
          become: yes
          tasks:
            - name: Install Tomcat 9
              apt:
                name:
                  - tomcat9
                  - tomcat9-admin
                state: present
                update_cache: yes

            - name: Copy WAR as ROOT.war
              copy:
                src: hello-web/target/hello.war
                dest: /var/lib/tomcat9/webapps/ROOT.war
                mode: '0644'

            - name: Restart Tomcat
              service:
                name: tomcat9
                state: restarted
        EOF

        ansible-playbook -i inventory.ini deploy.yml
        '''
      }
    }
  }
  post {
    success {
      echo "App URL: http://${APP_HOST}:8080/"
    }
  }
}
